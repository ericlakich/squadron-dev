# Compiled plugin binary
plugin
plugin.exe
# Bare `go build` in the module root names the binary after the module path's
# last element (the repo / directory name).
squadron-dev
*.exe
*.dll
*.so
*.dylib

# Build output
/dist/

# Local development workspaces created by the plugin at runtime
/.localdev-workspaces/

# Go
*.test
*.out

# Local environment / secrets — never commit credentials
.env
.env.local

# Claude Code local settings
.claude/settings.local.json

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
